# gb-verify matching & coverage parameters
# Override with: gb-verify --config path/to.toml ...

[match_config]
strong_min = 0.40
possible_min = 0.30

[coverage]
window_seconds = 2.0
hop_seconds = 0.5
window_score_min = 0.30

[verdict]
pass_coverage_min = 0.70
require_strong_match = true
